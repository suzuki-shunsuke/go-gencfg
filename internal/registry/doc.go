/*
Package registry converts implementations to interface and reduces each layer's dependencies.
If you want to switch implementions, you should do it with parameters of this layer's functions.
This layer basically is used by the command layer.
*/
package registry
